# 2026-04-01

## 游戏资产管理网站搭建

- 为 `/Users/huyi/dk_proj/game_assets` 搭建了完整的游戏资产管理网站
- 使用 Flask 后端 + 原生前端（HTML/CSS/JS）
- **后端** (`server.py`): 
  - 自动扫描 game_assets 目录（huahua/jrpg/xiaochu）
  - 提供文件 API：`/api/file/`（原图）、`/api/thumb/`（缩略图）、`/api/download/`（下载）
  - PIL 生成缩略图缓存到 `.thumbs/` 目录
  - 监听 `0.0.0.0:5050` 支持局域网共享
  - 局域网地址：`http://192.168.255.10:5050`
- **前端**: 暗色主题、卡片/列表双视图、搜索、排序、分页（80条/页）、图片预览、音频播放、下载
- **游戏项目**: 花花(🌸)、JRPG(⚔️)、消除(🧩)
- **资产分类**: 图片(png/jpg)、音频(mp3/wav/m4a)，支持子目录筛选

## 服务脚本化 & 项目 Skill

- 创建 `service.sh` 管理脚本：支持 start/stop/restart/status/log/follow
  - 后台运行（nohup），PID 文件 `.server.pid`，日志 `.server.log`
  - 自动检测端口占用并清理，优雅停止（5秒超时后 kill -9）
  - status 命令显示 PID、内存占用、运行时间、局域网地址
- `server.py` 开启 debug 自动重载：代码修改后自动重启，环境变量 `ASSET_DEBUG=0` 可关闭
- 创建项目 Skill 规则 `asset-manager.mdc`：记录项目结构、API、服务管理命令、开发约定
